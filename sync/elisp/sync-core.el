;;; sync-core.el --- Org/Reminders sync core primitives -*- lexical-binding: t; -*-

;;; Commentary:
;; Core helpers used by the batch sync entrypoint and tests.

;;; Code:

(defconst org-rem-sync-version "0.1.0"
  "Current sync module version.")

(provide 'sync-core)

;;; sync-core.el ends here
